# --- Dependencies ---
node_modules/

# --- Secrets & local env (commit `.env.example` instead) ---
.env
.env.local
.env.*.local
!.env.example

# Local-only Compose merge (never commit): Mac ARM64 vs Ubuntu AMD64 need different overrides.
docker-compose.override.yml

# --- WireGuard / Docker local state ---
# Docker bind mount (see root package.json `start` / `sudostart`)
/config/
config/

# Root-level legacy single-tunnel files (before per-tunnel layout)
/wg*.json
/wg*.conf

# Writable WG_PATH overrides used for local `node server.js` runs
.wg-data/
.wg-run/

# --- OS & editors ---
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
*.code-workspace
.vscode/
*.sublime-*

# --- Logs & caches ---
*.log
npm-debug.log*
.eslintcache
.npm
.yarn/
